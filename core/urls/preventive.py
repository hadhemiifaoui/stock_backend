
from rest_framework.routers import DefaultRouter
from ..views.preventive import PreventiveViewSet
from django.urls import path, include
from ..views.socket import all_preventive_alerts
router = DefaultRouter()

router.register(r'preventives',PreventiveViewSet , basename='preventives')

urlpatterns = [
    path('alerts/', all_preventive_alerts, name='alerts'),
    path('', include(router.urls)),
]


























































































































































