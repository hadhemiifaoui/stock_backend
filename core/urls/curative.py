
from rest_framework.routers import DefaultRouter
from ..views.curative import CurativeViewSet
from django.urls import path, include
from ..views.socket import curative_alerts
router = DefaultRouter()
router.register(r'curatives',CurativeViewSet , basename='curatives')


urlpatterns = [
    path('alerts/', curative_alerts, name='alerts'),
    path('', include(router.urls)),
]

























































































































































